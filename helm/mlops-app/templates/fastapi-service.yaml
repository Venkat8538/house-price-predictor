apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  labels:
    app: fastapi
spec:
  selector:
    app: fastapi
  ports:
  - protocol: TCP
    port: {{ .Values.fastapi.service.port }}
    targetPort: 8000
  type: {{ .Values.fastapi.service.type }}